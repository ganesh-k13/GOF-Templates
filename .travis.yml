language: python

python:
  - "3.6"

matrix:
  include:
    - os: linux
      sudo: required
      python: 3.6
      env: TOXENV=py36  
  include:
    - os: osx
      language: generic
      env: TOXENV=py36    
    
sudo: required

install: 
  - pip3 install -r requirements.txt

script:
  - pytest